<% if user_signed_in? && (current_user.user? || current_user.admin?)%>
  <% @usuario = current_user %>
  <% @usuario.estado_user = 1 %>
  <% @usuario.save %>
  <%link_to "Darme de baja", :method => dar_de_baja %>
<%end%>
